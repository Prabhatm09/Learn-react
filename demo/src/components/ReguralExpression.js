import React, { Component } from "react";

export class ReguralExpression extends Component{
    render(){
        return (
            <h1>Regual component -  {this.props.name}</h1>
        )
    }
}